apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "team-wireguard.fullname" . }}-config
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storage.config }}
